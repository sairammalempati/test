pipeline {
	agent any
	    stages {
	        stage('Clone Repository') {
	        /* Cloning the repository to our workspace */
	        steps {
	        checkout scm
	        }
	   }
	   stage('Build Image') {
	        steps {

	        }
	   }
	   stage('Run Image') {
	        steps {

	        }
	   }
	   stage('Testing'){
	        steps {

	            }
	   }
    }
}
